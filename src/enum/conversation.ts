export enum ConversationCategory {
    GROUP = 'GROUP',
    DIRECT = 'DIRECT'
}

export enum MessageCategory {
    TEXT = 'TEXT',
    PICTURE = 'PICTURE'
}

export enum ConversationParticipantRole {
    CITIZEN = 1,
    DICTATOR
}
